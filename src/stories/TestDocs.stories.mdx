<!-- Checkbox.stories.mdx -->

import { Canvas, Meta, Story } from "@storybook/addon-docs"

<!-- // import { HawaTypography } from "../../elements" -->

<Meta title="Test/Boga" />

<!-- // export const Template = (args) => <HawaTypography>tetet</HawaTypography> -->

# Checkbox

With `MDX`, we can define a story for `Checkbox` right in the middle of our
Markdown documentation.

<!--
<Canvas>
  <Story
    name="Unchecked"
    args={{
      label: 'Unchecked',
    }}>
    {Template.bind({})}
   </Story>

  <Story
    name="Checked"
    args={{
      label: 'Unchecked',
      checked: true,
    }}>
    {Template.bind({})}
   </Story>

  <Story
    name="Secondary"
    args={{
      label: 'Secondary',
      checked: true,
      appearance: 'secondary',
    }}>
    {Template.bind({})}
   </Story>
</Canvas> -->
