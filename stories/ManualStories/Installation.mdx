import { Meta, ArgsTable, Story, Unstyled, Markdown } from "@storybook/blocks";
import * as DialogStories from "../../stories/ElementsStories/Dialog.stories.tsx";
import { Button, DialogContent, CodeBlock } from "../../components/elements";
import { useDarkMode } from "storybook-dark-mode";

<Meta title="Installation" />

export const WrappedCodeBlock = (props) => {
  const isDark = useDarkMode();
  return (
    <CodeBlock
      language={props.lang}
      tabs={props.tabs}
      fileName={props.fileName}
      code={props.code}
    />

);
};

<div className="hawa-pb-4">
  <h1>Installation</h1>
  <p>Run one of the following commands to add Hawa to your project:</p>
  <Unstyled>
    <WrappedCodeBlock
      tabs={[
        {
          title: "npm",
          code: "npm install @sikka/hawa",
        },
        {
          title: "yarn",
          code: "yarn add @sikka/hawa",
        },
      ]}
    />
  </Unstyled>
</div>

---

### The npm package has 3 tags:

`@beta` for the archived hawa codebase (deprecated).

`@next`tag is used for the pre-release.

`@latest` latest stable release. (recommended)
