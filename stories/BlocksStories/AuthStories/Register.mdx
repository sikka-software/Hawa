import { ArgsTable } from "@storybook/blocks";

# RegisterForm

## Description

The `RegisterForm` component renders a form that allows users to register an account. It includes fields for personal and account information, and options for social media registration. It also provides a switch for language and color mode.

<ArgsTable
  exclude={[
    "texts",
    "viaGoogle",
    "viaGithub",
    "viaTwitter",
    "handleGoogleRegister",
    "handleTwitterRegister",
    "handleGithubRegister",
  ]}
/>

## 3rd Party Auth

<ArgsTable
  include={[
    "viaGoogle",
    "viaGithub",
    "viaTwitter",
    "handleGoogleRegister",
    "handleTwitterRegister",
    "handleGithubRegister",
  ]}
/>

## Usage

```jsx
import React from "react";
import { RegisterForm } from "your-library";

const MyComponent = () => {
  const handleRegister = (e) => {
    console.log("Form submitted:", e);
  };

  const texts = {
    fullNameLabel: "Full Name",
    emailLabel: "Email",
    passwordLabel: "Password",
    // ...other text labels
  };

  return (
    <RegisterForm
      texts={texts}
      handleRegister={handleRegister}
      viaGoogle={true}
      viaGithub={true}
      showTermsOption={true}
      // ...other props
    />
  );
};

export default MyComponent;
```

## Accessibility

Ensure all interactive elements are accessible by providing appropriate ARIA attributes, ensuring keyboard navigation, and other accessibility best practices.

## Styling

The `RegisterForm` component can be styled via CSS, using the class names specified in the component, such as `.hawa-flex`, `.hawa-flex-col`, `.hawa-gap-4`, etc.

## Customization

The `RegisterForm` component can be customized by passing different props like `viaGoogle`, `viaGithub`, `showTermsOption`, etc. This allows for a flexible registration form that can adapt to different requirements.

---

This documentation provides a clear understanding of what the `RegisterForm` component does, the props it accepts, and how to use it in a project. It also provides a basic usage example and some accessibility and styling information.
